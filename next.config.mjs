/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["dappunk.com", "api-inference.huggingface.co"], // Add the external domain here
  },
};

export default nextConfig;
